name: cron-test

on:
  schedule:
    - cron: '7/10 * * * *'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "UTC: $(date -u)"
          echo "event=${{ github.event_name }}"